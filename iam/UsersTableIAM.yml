UsersTableGET:
  Effect: "Allow"
  Action:
    - dynamodb:GetItem
  Resource: ${self:custom.usersTable.arn}

UsersTablePOST:
  Effect: "Allow"
  Action:
    - dynamodb:PutItem
  Resource: ${self:custom.usersTable.arn}
